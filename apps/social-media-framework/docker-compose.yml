version: '3.8'
services:
  social:
    image: node:20
    working_dir: /app
    volumes:
      - ./:/app
    command: ["pnpm", "run", "start:dev"]
    environment:
      - NODE_ENV=development
    ports: []
