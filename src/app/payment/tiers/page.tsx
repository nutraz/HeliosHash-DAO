'use client';

import CorrectedUPIPaymentGateway from '@/components/CorrectedUPIPaymentGateway';

/**
 * Renders the corrected UPI payment gateway component as the tiers page.
 *
 * @returns The React element displaying the corrected UPI payment gateway.
 */
export default function TiersPage() {
  return <CorrectedUPIPaymentGateway />;
}