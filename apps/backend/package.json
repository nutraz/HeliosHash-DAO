{
  "name": "helioshash_backend",
  "version": "0.0.0",
  "workspaces": [
    "src/helioshash_backend_frontend"
  ],
  "scripts": {
    "prebuild": "dfx generate helioshash_backend_backend",
    "build": "dfx build",
    "dev": "dfx start --background && npm run build",
    "pretest": "dfx generate helioshash_backend_backend",
    "test": "echo 'No tests yet'"
  }
}
