// Legacy mutation test file kept as placeholder to satisfy Vitest's test file discovery.
// Real comprehensive tests live in useMutations.test.tsx
describe('legacy useMutations placeholder', () => {
  it('placeholder passes', () => {
    expect(true).toBe(true);
  });
});
