// Generated type file stub — intentionally empty to avoid importing
// the project source during builds. Kept as a stub to satisfy
// the export pipeline without causing TypeScript/ESLint errors.

export {};
