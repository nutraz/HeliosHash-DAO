'use client';

import UPIPaymentGateway from '@/components/UPIPaymentGateway';

/**
 * Renders the UPI payment gateway page.
 *
 * @returns The UPI payment gateway React element.
 */
export default function UPIPage() {
  return <UPIPaymentGateway />;
}