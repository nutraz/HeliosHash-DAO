// Story moved to `apps/web/.storybook/stories/ProfileCard.stories.tsx`
// Kept placeholder to avoid Next compiling storybook imports inside `src`.
const _placeholder = null;
export default _placeholder;
